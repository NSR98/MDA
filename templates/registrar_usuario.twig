{% extends 'base/base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="card card-signin my-5" style="width: 1200px;">
                <div class="card-header">
                    <ul class="nav nav-tabs card-header-tabs nav-fill">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('modificar_perfil', {'id': usuario.id }) }}">Datos del usuario</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="">Cambiar contraseña</a>
                        </li>
                    </ul>
                </div>
                <div class="card-body">
                {% if registrar %}
                    <h5 class="card-title text-center">Unirse a Politicum</h5>
                {% else %}
                    <h5 class="card-title text-center">Modificar perfil</h5>
                {% endif %}
                    <div id="divForm" class="form-group">
                        {{ form(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}
    Registrar usuario
{% endblock %}

{% block js %}
    <script src="https://cdn.jsdelivr.net/npm/bs-custom-file-input/dist/bs-custom-file-input.min.js"></script>
    <script type="application/javascript">
        $(document).ready(function () {
            bsCustomFileInput.init();
        })
    </script>
{% endblock %}

